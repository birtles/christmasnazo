import React from 'react';
import { DataStore } from '../DataStore';

interface Props {
  dataStore: DataStore;
}

export const App: React.SFC<Props> = (props: Props) => {
  return <div>Yep</div>;
};
